locator: internal/config/parse_lua.go
meta:
  architecture_notes:
    - This file only parses sandbox policy; actual Lua execution lives under internal/stage.
    - Library toggles use explicit Has* flags so unset can differ from explicitly false.
  function_list:
    - parseLuaSandboxSection
  import_files_list:
    - cuelang.org/go/cue
  language: go
  purpose: Parse Lua sandbox settings that control script safety and deterministic behavior.
  responsibilities:
    - Decode Lua timeout, instruction, and memory limits.
    - Decode deterministicRandom and allowed standard-library toggles.
    - Preserve section and field presence for downstream validation and defaulting.
